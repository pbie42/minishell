Minishell:
	Builtins:
		☐ cd
		✔ env @done (8/20/2017, 12:40:21 AM)
		☐ setenv
		☐ unsetenv
		cd:
			☐ "cd -" (takes you back to the previously cd'd directory)
		setenv:
			☐ set a previously non existant envv
	Command Line:
		☐ Need to handle added tabs and spaces
